@using PagedList.Mvc
@model  PagedList.IPagedList<OptiMice_Assign.Models.category>

@{
    ViewBag.Title = "Category";
}

@*<h2>Index</h2>*@
<div class="container">
   
    <div class="row" style="margin-bottom:40px;">
        @if (Model != null)
        {
            foreach (var item in Model)
            {

                <div class="col-md-4" style="height:220px;margin-top:150px;">
                    <img src="@Url.Content(item.cat_img)" style="height:250px;width:100%;" />
                    <br />
                    @*<a href="#">*@<h4 style="color: black; font-family:'poppins'; text-align:center; margin-top:20px;">

                    @Html.ActionLink(@item.cat_name, "UDisplayAdd", new { id = item.cat_id })</h4>@*</a>*@
                    <br />
                  
                </div>
            }
        }
    </div>
    <div id="container">
        <div class="row" style="margin-top:150px;">

            <p></p>
            <p></p>
            <div class="col-md-4">
                <div class="pagination">
                    Page @(Model.PageCount<Model.PageNumber?0:Model.PageNumber)
                    of @Model.PageCount @Html.PagedListPager(Model, page => Url.Action("ViewCategory", new { page }))
                </div>
            </div>

        </div>
    </div>
</div>


